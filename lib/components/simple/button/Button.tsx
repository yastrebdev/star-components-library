import { Typography } from "components/primitive/typography"

const { Text } = Typography

export const Button = () => {
    return (
        <button>
            <Text>Button</Text>
        </button>
    )
}