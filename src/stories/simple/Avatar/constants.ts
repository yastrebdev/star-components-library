import { icons } from "components/primitive/icons/icons";
import { AvatarSize } from "components/simple/avatar/type";

export const options = {
    sizes: ['sm', 'md', 'lg'] as AvatarSize[],
    icons: [icons],
}