import { Meta, Canvas, Source, Title, Controls } from '@storybook/blocks'
import * as InputPasswordStories from './Password.stories'

<Meta of={InputPasswordStories} />

<Title children='Input.Password'/>
<Canvas of={InputPasswordStories.Default} sourceState='none'/>
<Source code={
`import { Input } from 'star-components-library';

const Password = Input.Password

const MyComponents = () => <Password />
`
} of={InputPasswordStories.Default} language="tsx"/>
<Controls />

<Title children='Visible Icon'/>
<Canvas of={InputPasswordStories.VisibleIcon} sourceState='none'/>
<Source code={
`import { Input } from 'star-components-library';

const Password = Input.Password

const MyComponents = () => {
    return (
        <Password 
            visibleIcon
            iconColor='#c09' 
            value='Please, click icon'
        />
    )
}
`
} of={InputPasswordStories.VisibleIcon} language="tsx"/>

<Title children='Visible Value'/>
<Canvas of={InputPasswordStories.VisibleValue} sourceState='none'/>
<Source code={
`import { Input } from 'star-components-library';

const Password = Input.Password

const MyComponents = () => {
    return (
        <Password 
            visibleIcon
            visibleValue
            value='Please, click icon'
        />
    )
}
`
} of={InputPasswordStories.VisibleValue} language="tsx"/>

<Title children='Change'/>
<Canvas of={InputPasswordStories.Change} sourceState='none'/>
<Source code={
`import { Input } from 'star-components-library';

const Password = Input.Password

const MyComponents = () => <Password onChange={(value) => console.log(value)}/>
`
} of={InputPasswordStories.Change} language="tsx"/>